import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'holamundo-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
